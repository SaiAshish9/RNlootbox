import React from 'react';
import {
  View,
  Dimensions,
  Text,
  ImageBackground,
  TouchableOpacity,
} from 'react-native';

const {width, height} = Dimensions.get('window');

const Drawer = () => {
  return (
    <View
      style={{
        display: 'flex',
        flexDirection: 'row',
        height,
        width,
      }}>
          
      </View>
  );
};

export default Drawer;
